meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{url}}/api/login
  body: json
  auth: none
}

body:json {
  {
    "email": "admin@example.com",
    "password": "secret123!"
  }
}

script:post-response {
  const token = res.body.token;
  bru.setEnvVar("token",token)
}
